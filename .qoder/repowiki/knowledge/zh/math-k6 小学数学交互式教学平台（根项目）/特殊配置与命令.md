开发：`npm run dev`；构建：`npm run build`（先执行 `tsc -b` 再 `vite build`）；预览：`npm run preview`；代码检查：`npm run lint`（Oxlint）；单元测试：`npm run test` / `npm run test:watch`（Vitest，jsdom 环境，setup 文件位于 `./src/test/setup.ts`）。