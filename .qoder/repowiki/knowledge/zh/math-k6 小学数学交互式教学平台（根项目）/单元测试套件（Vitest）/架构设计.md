测试文件按被测模块划分：content.test.ts 验证 `@/lib/content` 的知识点查询与详情加载；gameRunner.test.ts 验证 `@/components/GameRunner` 中的 `calculateStars` 评分函数；progress.test.ts 验证 `@/lib/progress` 的本地存储读写、关卡解锁与星级计算。setup.ts 作为全局初始化入口，仅导入 `@testing-library/jest-dom` 以启用额外断言。所有测试通过 `describe`/`it`/`expect` 组织，使用 `beforeEach` 清理 `localStorage` 保证用例隔离。